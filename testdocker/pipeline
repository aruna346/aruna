pipeline {
    agent any
 

    stages {
        stage('Build') {
            steps {
                dir('testdocker') {
                    sh 'ls'
                    sh 'docker build -t flask -f Dockerfile .'
                }
            }
        }
    }
}
